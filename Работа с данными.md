# Домашнее задание к занятию «Работа с данными (DDL/DML)»

**Фамилия Имя:** _Саушкин Николай Олегович_

---

## Задание 1

### 1.1. Поднимите чистый инстанс MySQL 8.0+

Использован MySQL Server 8.0.45 на Ubuntu 24.04 (локальный сервер, запущен из пакета `mysql-server`).

```bash
$ mysql --version
mysql  Ver 8.0.45-0ubuntu0.24.04.1 for Linux on x86_64 ((Ubuntu))

$ mysql -e "SELECT VERSION();"
+-------------------------+
| VERSION()               |
+-------------------------+
| 8.0.45-0ubuntu0.24.04.1 |
+-------------------------+
```

### 1.2. Создайте учётную запись `sys_temp`

```sql
CREATE USER 'sys_temp'@'localhost' IDENTIFIED BY 'Password123!';
```

### 1.3. Список пользователей в БД (скриншот)

```sql
SELECT User, Host FROM mysql.user;
```

<img width="368" height="208" alt="image" src="https://github.com/user-attachments/assets/e3fb3e55-ed00-4948-90d6-a85b96191aa0" />


### 1.4. Дайте все права для пользователя `sys_temp`

```sql
GRANT ALL PRIVILEGES ON *.* TO 'sys_temp'@'localhost' WITH GRANT OPTION;
FLUSH PRIVILEGES;
```

### 1.5. Список прав для пользователя `sys_temp` (скриншот)

```sql
SHOW GRANTS FOR 'sys_temp'@'localhost';
```

<img width="466" height="551" alt="image" src="https://github.com/user-attachments/assets/06d4012c-997a-41f6-8ab5-eb4400372d3b" />  

### 1.6. Переподключитесь от имени `sys_temp`

```bash
$ mysql -u sys_temp -p
Enter password: ********

mysql> SELECT CURRENT_USER(), USER();
+--------------------+--------------------+
| CURRENT_USER()     | USER()             |
+--------------------+--------------------+
| sys_temp@localhost | sys_temp@localhost |
+--------------------+--------------------+
```

### 1.7. Скачайте и восстановите дамп `sakila`

```bash
# Скачивание
$ wget https://downloads.mysql.com/docs/sakila-db.zip
$ unzip sakila-db.zip
$ cd sakila-db/

# Восстановление
$ mysql -u sys_temp -p < sakila-schema.sql
$ mysql -u sys_temp -p < sakila-data.sql

# Проверка
mysql> SHOW DATABASES;
+--------------------+
| Database           |
+--------------------+
| information_schema |
| mysql              |
| performance_schema |
| sakila             |
| sys                |
+--------------------+
```

### 1.8. ER-диаграмма / список таблиц базы (скриншот)

**ER-диаграмма** (получена через `eralchemy2`):

<img width="3469" height="1549" alt="image" src="https://github.com/user-attachments/assets/5a5fc309-925a-4221-80a1-f574f5fdc692" />

**Список таблиц** через командную строку:

```sql
USE sakila;
SHOW TABLES;
```

<img width="391" height="508" alt="image" src="https://github.com/user-attachments/assets/2138d0e0-4071-4b13-9513-46fc2f227957" />


### Простыня со всеми запросами Задания 1

```sql
-- 1.2
CREATE USER 'sys_temp'@'localhost' IDENTIFIED BY 'Password123!';

-- 1.3
SELECT User, Host FROM mysql.user;

-- 1.4
GRANT ALL PRIVILEGES ON *.* TO 'sys_temp'@'localhost' WITH GRANT OPTION;
FLUSH PRIVILEGES;

-- 1.5
SHOW GRANTS FOR 'sys_temp'@'localhost';

-- 1.6
-- shell: mysql -u sys_temp -p
SELECT CURRENT_USER(), USER();
-- (опционально, если нужен mysql_native_password)
-- ALTER USER 'sys_temp'@'localhost'
--     IDENTIFIED WITH mysql_native_password BY 'Password123!';

-- 1.7
-- shell: wget https://downloads.mysql.com/docs/sakila-db.zip
-- shell: unzip sakila-db.zip && cd sakila-db
-- shell: mysql -u sys_temp -p < sakila-schema.sql
-- shell: mysql -u sys_temp -p < sakila-data.sql
SHOW DATABASES;

-- 1.8
USE sakila;
SHOW TABLES;
```

---

## Задание 2

Таблица из двух столбцов: имена таблиц базы `sakila` и их первичные ключи.

Запрос, которым она была получена:

```sql
SELECT
    t.TABLE_NAME AS 'Название таблицы',
    COALESCE(
        GROUP_CONCAT(k.COLUMN_NAME ORDER BY k.ORDINAL_POSITION SEPARATOR ', '),
        '— (VIEW, без PK)'
    ) AS 'Название первичного ключа'
FROM information_schema.TABLES t
LEFT JOIN information_schema.KEY_COLUMN_USAGE k
    ON t.TABLE_SCHEMA = k.TABLE_SCHEMA
   AND t.TABLE_NAME   = k.TABLE_NAME
   AND k.CONSTRAINT_NAME = 'PRIMARY'
WHERE t.TABLE_SCHEMA = 'sakila'
GROUP BY t.TABLE_NAME
ORDER BY t.TABLE_NAME;
```

| Название таблицы              | Название первичного ключа |
|-------------------------------|---------------------------|
| actor                         | actor_id                  |
| actor_info                    | — (VIEW, без PK)          |
| address                       | address_id                |
| category                      | category_id               |
| city                          | city_id                   |
| country                       | country_id                |
| customer                      | customer_id               |
| customer_list                 | — (VIEW, без PK)          |
| film                          | film_id                   |
| film_actor                    | actor_id, film_id         |
| film_category                 | film_id, category_id      |
| film_list                     | — (VIEW, без PK)          |
| film_text                     | film_id                   |
| inventory                     | inventory_id              |
| language                      | language_id               |
| nicer_but_slower_film_list    | — (VIEW, без PK)          |
| payment                       | payment_id                |
| rental                        | rental_id                 |
| sales_by_film_category        | — (VIEW, без PK)          |
| sales_by_store                | — (VIEW, без PK)          |
| staff                         | staff_id                  |
| staff_list                    | — (VIEW, без PK)          |
| store                         | store_id                  |

> 7 объектов из 23 — это представления (VIEW), у них нет первичного
> ключа: `actor_info`, `customer_list`, `film_list`,
> `nicer_but_slower_film_list`, `sales_by_film_category`,
> `sales_by_store`, `staff_list`.
>
> Две таблицы (`film_actor`, `film_category`) имеют составной
> первичный ключ из двух колонок.

